# Virtual environments
flimpa_env/
.venv
.venv/
.venv-icloud-broken/

# Python bytecode
__pycache__/
*.py[cod]

# Test / IDE / OS caches
.pytest_cache/
.idea/
.DS_Store

# PyInstaller output
build/
dist/
FLIMPA (v1.0).spec
FLIMPA.spec

# Offline wheel bundles are large; keep scripts/docs, not binary wheels
offline/wheels-*/

# Large FLIMFIT installers / app bundles (exceed GitHub file size limits)
FLIMFIT/*.app/
FLIMFIT/*.dmg
*.dmg

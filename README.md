# Flimpa---modifications
Flimpa with extra functions (masking, FRET, baseline check). See `docs/README.md`.

## Run
```bash
pip install -r requirements.txt
python main.py
```
Or open the folder in PyCharm and install from `requirements.txt`, then run `main.py`.

